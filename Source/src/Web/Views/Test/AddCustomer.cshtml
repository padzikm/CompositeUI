@using CompositeUI.Web.Common.UIKeys
@model IDictionary<string, CompositeUI.Service.Infrastructure.ViewModels.IViewModel>

@{
    ViewBag.Title = "title";
}

<h2>main action</h2>

@using (Html.BeginForm())
{
    <input type="hidden" name="testId" value="@ViewBag.Id"/>
    <div>
        @Model[UIKeysTest.AddCustomer.PersonalData].Execute(this)
    </div>
    <div>
        @Model[UIKeysTest.AddCustomer.AgeData].Execute(this)
    </div>
    <input type="submit" value="Send"/>
}
